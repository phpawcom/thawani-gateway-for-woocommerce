<?php
/**
 * Intentionally empty: uninstalling the plugin should leave gateway settings,
 * the wp_thawani_invoice_map table and order/user meta in place so the merchant
 * can re-enable the plugin without losing existing data.
 */

defined( 'WP_UNINSTALL_PLUGIN' ) || exit;
